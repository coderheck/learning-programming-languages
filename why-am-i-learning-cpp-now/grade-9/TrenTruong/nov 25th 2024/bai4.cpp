#include"iostream"
#include"algorithm"
using namespace std;
#define ll long long
#define maxN 100000
struct pos{ll x,time;}a[maxN+1];
bool cmpfn(const pos &a,const pos &b){
  
}
int n;ll t;
int main(){
  cin>>n;
  for(int i=0;i<n;i++){
    cin>>a[i].x>>a[i].time;
  }
  sort(a,a+n);
}