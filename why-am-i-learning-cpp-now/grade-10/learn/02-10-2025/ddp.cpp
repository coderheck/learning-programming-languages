#include <cstring>
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;
#define ll long long 
string s;ll dp[20][2][150]; // max num length, state, sum
ll qhdcs(string s,ll idx,ll tt,ll sum){
	
}
void sol(){
	reverse(s.begin(),s.end());
	memset(dp,-1,sizeof(dp));

}
int main(){
	cin.tie(0)->sync_with_stdio(0);
	cin>>s;
	// vector<vector<vector<ll>>>dp(16,vector<vector<ll>>(2,vector<ll>(150,-1))); // max num length, state, sum
	
}
